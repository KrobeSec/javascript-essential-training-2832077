class HockeyBag {
  constructor(
    // define parameters
    brand,
    size,
    colour,
    pockets
  ) {
    // define properties
    this.brand = brand;
    this.size = size;
    this.colour = colour;
    this.pockets = pockets;
  }
}

export default HockeyBag;
